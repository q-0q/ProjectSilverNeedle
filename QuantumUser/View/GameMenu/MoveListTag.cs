using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveListTag : MonoBehaviour
{
    public enum Type
    {
        Launches,
        GroundBounces,
        WallBounces,
        HardKnockdown,
        Low,
        High,
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
